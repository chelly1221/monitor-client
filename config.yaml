# USB HID Relay Network Controller Configuration

# USB Relay Device Settings
relay:
  # Device serial number (auto-detected if omitted)
  # serial_number: "AFED5"

  # Number of channels on the relay board (auto-detected from product string if omitted)
  # channels: 8

  # Automatic reconnection if device disconnects
  auto_reconnect: true

  # Seconds between reconnection attempts (exponential backoff applied)
  reconnect_interval: 5

# Network Server Settings
network:
  # TCP Server Configuration
  tcp:
    enabled: true
    host: "0.0.0.0"  # Listen on all interfaces
    port: 5000       # TCP port

  # UDP Server Configuration
  udp:
    enabled: true
    host: "0.0.0.0"  # Listen on all interfaces
    port: 5001       # UDP port

# Logging Settings
logging:
  # Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
  level: "INFO"

  # Log file path (set to null to disable file logging)
  file: "/var/log/relay-controller.log"

  # Maximum log file size in MB before rotation
  max_size_mb: 10

  # Number of backup log files to keep
  backup_count: 3
